useDynLib(cydar, .registration=TRUE, .fixes="cxx_")

import(BiocParallel)
importFrom("plotrix", color.scale)
importFrom("viridis", viridis)
importFrom("glmnet", glmnet)
importFrom("methods", is)
importFrom("stats", coef, kmeans, median, quantile, lm.fit, approxfun, density)

importFrom("graphics", plot, points)
importFrom("shiny", 
        column, fluidPage, fluidRow, pageWithSidebar, sidebarPanel, mainPanel, headerPanel, h4, hr,
        observeEvent, actionButton, textInput,
        plotOutput, renderPlot, renderTable, tableOutput,
        shinyApp, stopApp)

export(countCells,  
    expandRadius,
    quantileBatch,
    plotCellLogFC,
    plotCellIntensity,
    intensityRanges,
    spatialFDR,
    pickBestMarkers,
    prepareCellData,
    neighborDistances,
    findFirstSphere,
    interpretSpheres
)

