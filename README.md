# cydar
Using mass cytometry for differential abundance analyses in R.

- To install the package, use `devtools::intall_github("LTLA/cydar")`
- Read the vignette with `vignette(package="cydar")` for how to construct a workflow.
